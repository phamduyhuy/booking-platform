server:
  port: 8036
  servlet:
    context-path: /ai
  tomcat:
    max-http-form-post-size: 20MB
    max-swallow-size: 20MB
<<<<<<< HEAD
    connection-timeout: 600000  # 10 minutes in milliseconds
    threads:
      max: 200
      min-spare: 10
  # WebSocket configuration for long AI processing
  websocket:
    max-idle-timeout: 600000  # 10 minutes
    max-text-message-buffer-size: 1048576  # 1MB
    max-binary-message-buffer-size: 1048576  # 1MB

  # Vector store configuration for default profile

# OpenAPI Documentation Configuration
#springdoc:
#  api-docs:
#    path: /api/docs
#    enabled: true
#  swagger-ui:
#    path: /api/swagger-ui
#    enabled: true
#    operationsSorter: method
#    tagsSorter: alpha
#    try-it-out-enabled: true
#  show-actuator: true

=======
keycloak:
  client-id: ai-agent
  client-secret: ${BOOKING_SMART_AI_CLIENT_SECRET}
  token-uri: ${KEYCLOAK_HOST}/realms/BookingSmart/protocol/openid-connect/token
>>>>>>> origin/dev
spring:
  profiles:
    active: local
  application:
    name: aiAgent-service
  threads:
    virtual:
      enabled: true

  # Servlet multipart configuration for large file uploads
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.lob.non_contextual_creation: true
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: bookingsmart-rag-service
      auto-offset-reset: latest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3
  ai:
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: never
langchain4j:
  google-ai-gemini:

    chat-model:
      api-key: ${GEMINI_API_KEY}
      model-name: gemini-2.5-flash
      temperature: 0.1

    streaming-chat-model:
      api-key: ${GEMINI_API_KEY}
      model-name: gemini-2.5-flash
      temperature: 0.1


---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_db
    username: postgres
    password: p0stgr3s
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  elasticsearch:
    uris: ${ELASTIC_URIS}
    username: ${ELASTIC_USERNAME}
    password: ${ELASTIC_PASSWORD}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: bookingsmart-rag-service
      auto-offset-reset: latest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3
  ai:
    mcp:
      client:
        enabled: true
        name: bookingsmrt-mcp-client
        version: 1.0.0

        request-timeout: 300s
        type: sync
        root-change-notification: true
        toolcallback.enabled: true
        sse:
          connections:
            server1:
              url: http://localhost:8082
              sse-endpoint: /hotels/mcp
            server2:
              url: http://localhost:8081
<<<<<<< HEAD
              sse-endpoint: /flights/sse
            customer-server:
              url: http://localhost:8084
              sse-endpoint: /customers/sse
            payment-server:
              url: http://localhost:8085
              sse-endpoint: /payments/sse
=======
              sse-endpoint: /flights/mcp
>>>>>>> origin/dev
        stdio:
          connections:
            openweather:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - OWM_API_KEY
                - mcp/openweather
              env:
                OWM_API_KEY: ${OWM_API_KEY}
            mapbox:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - MAPBOX_ACCESS_TOKEN
                - mcp/mapbox
              env:
                MAPBOX_ACCESS_TOKEN: ${MAPBOX_ACCESS_TOKEN}
            brave:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - BRAVE_API_KEY
                - mcp/brave-search
              env:
                BRAVE_API_KEY: ${BRAVE_API_KEY}
            stripe:
              command: docker
              args:
                - run
                - --rm
                - -i
                - -e
                - STRIPE_SECRET_KEY
                - mcp/stripe
                - --tools=all
              env:
                STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
        initialized: true
  # Service URLs for RAG initialization
  app:
    flight-service:
      url: http://localhost:8081
    hotel-service:
      url: http://localhost:8082
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/BookingSmart
      client:
        registration:
          my-client1:
            provider: auth-server
            client-id: ai-agent
            client-secret: ${BOOKING_SMART_AI_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            scope:
              - mcp
        provider:
          auth-server:
            token-uri: http://localhost:9090/realms/BookingSmart/protocol/openid-connect/token
---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: redis
      port: 6379
      password:
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  elasticsearch:
    uris: ${ELASTIC_URIS}
  


  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: bookingsmart-rag-service
      auto-offset-reset: latest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI}
      client:
        registration:
          my-client1:
            provider: auth-server
            client-id: ai-agent
            client-secret: ${BOOKING_SMART_AI_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            scope:
              - mcp
        provider:
          auth-server:
            token-uri: ${KEYCLOAK_HOST}/realms/BookingSmart/protocol/openid-connect/token

  ai:
<<<<<<< HEAD
    retry:
      exclude-on-http-codes: 429
      on-client-errors: true
    model:
      embedding: ollama

    vectorstore:
      elasticsearch:
        dimensions: 768
        similarity: cosine
        initialize-schema: true

    ollama:
      base-url: http://ollama:11434
      embedding:
        options:
          model: nomic-embed-text:latest
    mistralai:
      api-key: ${MISTRAL_API_KEY}
      chat:
        options:

          model: mistral-medium-2508
          temperature: 0.0
    google:
      genai:
        api-key: ${GEMINI_API_KEY}

        chat:
          options:
            model: gemini-2.5-pro
            temperature: 0.3
            use-cached-content: true
            auto-cache-ttl: 86400  # 1 day in seconds



=======
>>>>>>> origin/dev
    mcp:
      client:
        enabled: true
        version: 1.0.0
        type: sync
        request-timeout: 300s
        name: bookingsmart
        root-change-notification: true
        toolcallback.enabled: true
        
        sse:
          connections:
            hotel-mcp-server:
              url: http://hotel-service
              sse-endpoint: /hotels/mcp
            flight-mcp-server:
              url: http://flight-service
<<<<<<< HEAD
              sse-endpoint: /flights/sse
            customer-mcp-server:
              url: http://customer-service
              sse-endpoint: /customers/sse
            booking-mcp-server:
              url: http://booking-service
              sse-endpoint: /bookings/sse
            payment-mcp-server:
              url: http://payment-service
              sse-endpoint: /payments/sse
=======
              sse-endpoint: /flights/mcp
            # booking-mcp-server:
            #   url: http://booking-service
            #   sse-endpoint: /bookings/sse
            # payment-mcp-server:
            #   url: http://payment-service
            #   sse-endpoint: /payments/sse
>>>>>>> origin/dev
        
        stdio:
          connections:
            openweather:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - OWM_API_KEY
                - mcp/openweather
              env:
                OWM_API_KEY: ${OWM_API_KEY}
            mapbox:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - MAPBOX_ACCESS_TOKEN
                - -e
                - MCP_DISABLE_LOGGING
                - mcp/mapbox
              env:
                MAPBOX_ACCESS_TOKEN: ${MAPBOX_ACCESS_TOKEN}
                MCP_DISABLE_LOGGING: false
            brave:
              command: docker
              args:
                - run
                - -i
                - --rm
                - -e
                - BRAVE_API_KEY
                - -e
                - BRAVE_MCP_DISABLED_TOOLS
                - mcp/brave-search
              env:
                BRAVE_API_KEY: ${BRAVE_API_KEY}
                BRAVE_MCP_DISABLED_TOOLS: brave_news_search brave_web_search brave_summarizer	
        initialized: true




app:
  flight-service:
    url: http://flight-service
  hotel-service:
    url: http://hotel-service


eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI}
  instance:
    prefer-ip-address: true